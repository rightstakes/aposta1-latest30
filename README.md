
  # Convert Design to Production

  This is a code bundle for Convert Design to Production. The original project is available at https://www.figma.com/design/n7sMYHPILOUkLIC8vxk7yM/Convert-Design-to-Production.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
